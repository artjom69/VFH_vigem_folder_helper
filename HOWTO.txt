

a copy timestamps algorithm for vigem recording data.

    -check your OS, 32, or 64 bit
    -copy VFH to vigem hard drive
    -start the .exe file, if error message, try to install the C++ runtime from driver folder
    -start recording
    -usage of VFH is self-explanatory

